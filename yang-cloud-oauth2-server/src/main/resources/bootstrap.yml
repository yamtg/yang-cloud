spring:
  application:
    name: yang-cloud-oauth2-server
  profiles:
    active: dev
server:
  port: 8762
  context-path: /oauth2
management:
  security:
    enabled: false
  context-path: /oauth2
  port: 9001
